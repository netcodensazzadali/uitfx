import React from 'react'

const SectionTitleFlip = (props) => {
  return (
    <div className='SectionTitleFlip'>
        <span>{props.headingSubTitle}</span>
        <h2>{props.headingtitle}</h2>
    </div>
  )
}

export default SectionTitleFlip